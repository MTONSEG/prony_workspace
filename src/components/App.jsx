import React, { useState } from "react";
import './App.scss';

const App = ({ store }) => {
	let [state, setState] = useState(store);

	return (
		<div className="wrapper">
			
		</div>
	)
}

export default App;
